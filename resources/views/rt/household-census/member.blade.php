<fieldset class="card border shadow-sm mb-4" data-member>
    <legend class="visually-hidden">Anggota <span data-member-number>{{ is_numeric($index) ? ((int) $index + 1) : '' }}</span></legend>
    <div class="card-header bg-light d-flex justify-content-between align-items-center gap-3"><strong class="text-primary text-uppercase"><i class="bi bi-person me-2" aria-hidden="true"></i>Anggota <span data-member-number>{{ is_numeric($index) ? ((int) $index + 1) : '' }}</span></strong><button class="btn btn-outline-danger btn-sm" type="button" data-remove-member aria-label="Hapus anggota {{ is_numeric($index) ? ((int) $index + 1) : '' }}"><i class="bi bi-trash me-1" aria-hidden="true"></i>Hapus</button></div>
    <div class="card-body p-3 p-lg-4"><div class="row g-3"><div class="col-md-6"><label class="form-label" for="member-{{ $index }}-name">Nama <span class="text-danger">*</span></label><input id="member-{{ $index }}-name" class="form-control @error('members.'.$index.'.name') is-invalid @enderror" name="members[{{ $index }}][name]" value="{{ $person['name'] ?? '' }}" required>@error('members.'.$index.'.name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div>
    <div class="col-md-6"><label class="form-label" for="member-{{ $index }}-relationship">Hubungan dalam keluarga <span class="text-danger">*</span></label><select id="member-{{ $index }}-relationship" class="form-select @error('members.'.$index.'.family_relationship') is-invalid @enderror" name="members[{{ $index }}][family_relationship]" required><option value="">Pilih</option>@foreach($relationships as $relationship)<option value="{{ $relationship->value }}" @selected(($person['family_relationship'] ?? '') === $relationship->value)>{{ $relationship->label() }}</option>@endforeach</select>@error('members.'.$index.'.family_relationship')<div class="invalid-feedback">{{ $message }}</div>@enderror</div>
    @include('rt.household-census.person-fields', ['prefix' => 'members.'.$index, 'person' => $person, 'idPrefix' => 'member-'.$index])</div></div>
</fieldset>
