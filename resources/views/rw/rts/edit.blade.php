@extends('layouts.app') @section('title','Edit RT - SIGAP WARGA') @section('content')<main class="container py-4" style="max-width:760px"><nav aria-label="breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('rw.dashboard') }}">Dashboard</a></li><li class="breadcrumb-item"><a href="{{ route('rw.rts.index') }}">Kelola RT</a></li><li class="breadcrumb-item active">{{ $managedRt->code }}</li></ol></nav><h1 class="h2 mb-4">Edit RT</h1>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif @error('status')<div class="alert alert-danger">{{ $message }}</div>@enderror<div class="card border-0 shadow-sm mb-4"><form method="POST" action="{{ route('rw.rts.update',$managedRt) }}">@csrf @method('PUT')<div class="card-body p-4">@include('rw.rts._form')</div><div class="card-footer bg-white p-4"><button class="btn btn-primary">Simpan</button></div></form></div><div class="card border-0 shadow-sm mb-4"><div class="card-body p-4 d-flex flex-wrap justify-content-between align-items-center gap-3"><div><h2 class="h5 mb-1">Petugas RT</h2><p class="text-secondary mb-0">Buat, aktif/nonaktifkan, dan reset akun petugas untuk RT ini.</p></div><a class="btn btn-outline-primary" href="{{ route('rw.rts.officers.index',$managedRt) }}">Kelola Petugas RT</a></div></div><div class="card border-0 shadow-sm"><div class="card-body p-4"><h2 class="h5">Status RT</h2><p class="text-secondary">RT dengan akun petugas aktif tidak dapat dinonaktifkan.</p><form method="POST" action="{{ route('rw.rts.status.toggle',$managedRt) }}">@csrf @method('PATCH')<button class="btn btn-{{ $managedRt->is_active?'outline-danger':'success' }}">{{ $managedRt->is_active?'Nonaktifkan RT':'Aktifkan RT' }}</button></form></div></div></main>@endsection
