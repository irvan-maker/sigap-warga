@extends('layouts.app') @section('title','Edit RW - SIGAP WARGA') @section('content')<main class="container py-4" style="max-width:760px"><nav aria-label="breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('kelurahan.dashboard') }}">Dashboard</a></li><li class="breadcrumb-item"><a href="{{ route('kelurahan.rws.index') }}">Kelola RW</a></li><li class="breadcrumb-item active">{{ $managedRw->code }}</li></ol></nav><h1 class="h2 mb-4">Edit RW</h1>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif @error('status')<div class="alert alert-danger">{{ $message }}</div>@enderror<div class="card border-0 shadow-sm mb-4"><form method="POST" action="{{ route('kelurahan.rws.update',$managedRw) }}">@csrf @method('PUT')<div class="card-body p-4">@include('kelurahan.rws._form')</div><div class="card-footer bg-white p-4"><button class="btn btn-primary">Simpan</button></div></form></div><div class="card border-0 shadow-sm"><div class="card-body p-4"><h2 class="h5">Status RW</h2><p class="text-secondary">RW memiliki {{ $managedRw->rts_count }} RT. RW dengan RT aktif atau akun wilayah aktif tidak dapat dinonaktifkan.</p><form method="POST" action="{{ route('kelurahan.rws.status.toggle',$managedRw) }}">@csrf @method('PATCH')<button class="btn btn-{{ $managedRw->is_active?'outline-danger':'success' }}">{{ $managedRw->is_active?'Nonaktifkan RW':'Aktifkan RW' }}</button></form></div></div></main>@endsection
